import { HigthLigthDirective } from './higth-ligth.directive';

describe('HigthLigthDirective', () => {
  it('should create an instance', () => {
    const directive = new HigthLigthDirective();
    expect(directive).toBeTruthy();
  });
});
